<div class="ajax-img">
   <!--  @if($productImg)
        @foreach($productImg as $item)
        <div>
            <img style="width: 150px;border:1px solid #555" src="{{asset('uploads/image_product/'.$item->image)}}" />
            <a href="" data="{{$item->id}}" url="{{route('productImage.delete')}}">
                <i class="fas fa-trash-alt"></i>
            </a>
        </div>
        @endforeach
    @endif -->
    <p>avb</p>
</div>